name: Publish to NuGet

on:
  workflow_dispatch:

env:
  DOTNET_TARGET: 'net10.0'

jobs:
  build-pack-and-push:
    runs-on: ubuntu-latest
    
    permissions:
      id-token: write # enable GitHub OIDC token
    
    steps:
      - name: Checkout source code
        uses: actions/checkout@v6
        with:
          sparse-checkout: src
          sparse-checkout-cone-mode: false

      - name: Setup .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: '10.0.x'
          dotnet-quality: 'ga'

      - name: Build DaJet Scripting
        working-directory: src/dajet-scripting
        run: dotnet build --configuration Release

      - name: Pack DaJet Scripting
        working-directory: src/dajet-scripting
        run: dotnet pack --configuration Release --no-build --output ../nupkg

      - name: Build DaJet Interpreter
        working-directory: src/dajet-interpreter
        run: dotnet build --configuration Release

      - name: Pack DaJet Interpreter
        working-directory: src/dajet-interpreter
        run: dotnet pack --configuration Release --no-build --output ../nupkg

      - name: Build DaJet Script Host
        working-directory: src/dajet-host
        run: dotnet build --configuration Release

      - name: Pack DaJet Script Host
        working-directory: src/dajet-host
        run: dotnet pack --configuration Release --no-build --output ../nupkg
        
      - name: NuGet login (OIDC)
        uses: NuGet/login@v1
        id: login
        with:
          user: ${{secrets.NUGET_USER}}
    
      - name: NuGet push
        working-directory: src
        run: dotnet nuget push ./nupkg/*.nupkg --api-key ${{steps.login.outputs.NUGET_API_KEY}} --source https://api.nuget.org/v3/index.json --skip-duplicate
